[mysqld]
# Unique ID for this server in the replication topology
server-id         = 1

# Enable binary logging — required for Master role
log_bin           = mysql-bin
binlog_format     = ROW
binlog_do_db      = appdb

# Retain binlog files for 7 days
expire_logs_days  = 7
