[mysqld]
server-id                  = 1

log_bin                    = mysql-bin
binlog_format              = ROW
binlog_do_db               = appdb
expire_logs_days           = 7

# GTID — enables position-free replication (slave uses AUTO_POSITION=1)
gtid_mode                  = ON
enforce_gtid_consistency   = ON
